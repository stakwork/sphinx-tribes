// describe('Create Workspace', () => {
//   it('passes', () => {
//     cy.request({
//       method: 'POST',
//       url: `${hostname}/workspaces/`,
//       headers: {
//         'x-user-token': `${user.authToken}`
//       },
//       body: workspaces[0]
//     }).then((response) => {
//       const id = response.body.response.id;
//     })
//   })
// });